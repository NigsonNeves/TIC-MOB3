onPress={() =>navigate('Graph', { name: 'Graph' })

Expo.FileSystem.documentDirectory

DocumentPicker.getDocumentAsync(options)

try {
  const { uri } = await downloadResumable.downloadAsync();
  console.log('Finished downloading to ', uri);
} catch (e) {
  console.error(e);
}